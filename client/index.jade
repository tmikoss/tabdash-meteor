head
  title Tabdash
body
  +header
  +weather
  
template(name='header')
  .row
    .col-xs-3
    .col-xs-6.text-center
      .current-time
        = formattedTime
      .current-date.text-muted
        = formattedDate
    .col-xs-3.namedays
      each namedays
        .text-muted
          = name

template(name='weather')
  table.table.forecasts
    thead
      tr
        th
        th °C
        th
    tbody
      each forecasts
        +weatherRow

template(name='weatherRow')
  tr
    td.text-center.text-muted
      = range
    td.text-center
      strong
        = temperature
    td.text-left
      img(src="http://api.yr.no/weatherapi/weathericon/1.1/?symbol=#{code};content_type=image/png")
      span.text-muted
        = name

//-   %slide
//-     .slide-content{ 'ng-controller' => 'RouteGroupsCtrl' }
//-       .row
//-         .route-group{ 'ng-repeat' => "routeGroup in routeGroups | orderBy : 'priority' " }
//-           %h1
//-             {{ routeGroup.name }}
//-             %span.text-muted
//-               {{ routeGroup.upcoming[0].time.fromNow() }}
//-           .run.row{ 'ng-repeat' => 'run in routeGroup.upcoming'}
//-             .col-xs-4.text-right.col-xs-offset-1
//-               {{ run.number }}
//-               %img{ 'ng-src' => 'images/{{ run.kind }}.png' }
//-             .col-xs-6.time.text-left
//-               {{ run.time.fromNow() }}
//- 
//-   %slide
//-     .slide-content{ 'ng-controller' => 'TrainsCtrl' }
//-       %table.table.trains
//-         %thead
//-           %tr
//-             %th
//-             %th Dole
//-             %th Salaspils
//-         %tbody
//-           %tr{ 'ng-repeat' => "train in trains | orderBy : 'time' " }
//-             %td.text-muted
//-               {{ train.time | date : 'HH:mm' }}
//-             %td
//-               %span.glyphicon.glyphicon-ok{ 'ng-if' => 'train.dole' }
//-             %td
//-               %span.glyphicon.glyphicon-ok{ 'ng-if' => 'train.salaspils' }
